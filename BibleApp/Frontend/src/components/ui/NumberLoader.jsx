import styles from "../../styles/NumberLoader.module.css";

function NumberLoader() {
    return (
        <>
            <span className={styles.loading_dash}>—</span>
        </>
    );
}

export default NumberLoader;